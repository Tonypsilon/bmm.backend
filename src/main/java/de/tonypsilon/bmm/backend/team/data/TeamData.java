package de.tonypsilon.bmm.backend.team.data;

public record TeamData(Long id,
                       Long seasonId,
                       Long organizationId,
                       Integer number) {
}
