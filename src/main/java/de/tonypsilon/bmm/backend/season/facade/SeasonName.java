package de.tonypsilon.bmm.backend.season.facade;

/**
 * Container objects like this for simple Strings are
 * used to work with correct JSON on the API level for simple Strings.
 */
public record SeasonName(String seasonName) {
}
